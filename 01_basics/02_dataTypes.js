"use strict"; // treat all JS code as newer version

// alert(3+3)  // we are using nodejs, not browser or html

console.log(3 + 3); // code readability should be high

console.log("Anmol");

let name = "Anmol";
let age = 18;
let isLoggedIn = false;

// number => 2 to power 53 range
// bigint => for big number like used in trading
// string => ""
// boolean => true/false
// null => standalone value
// undefined =>
// symbol => unique

// object

console.log(typeof isLoggedIn);
console.log(typeof undefined); // undefined (this will be output)
console.log(typeof null); // object (this will be output)
